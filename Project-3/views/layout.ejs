<!DOCTYPE html>
<html>
<head>
    <title>Mini Instagram</title>

    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f5f5f5;
            margin: 0;
        }

        nav {
            background: white;
            padding: 15px 0;
            border-bottom: 1px solid #ddd;
        }

        .nav-container {
            max-width: 800px;
            margin: auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        nav a {
            text-decoration: none;
            color: black;
            margin-left: 15px;
            font-weight: 500;
        }

        .container {
            max-width: 600px;
            margin: 40px auto;
        }

        h2 {
            text-align: center;
        }

        .card {
            background: white;
            border: 1px solid #ddd;
            border-radius: 8px;
            margin-bottom: 25px;
            overflow: hidden;
        }

        .card img {
            width: 100%;
            display: block;
        }

        .card-content {
            padding: 15px;
        }

        .delete-btn {
            display: inline-block;
            margin-top: 10px;
            padding: 6px 12px;
            background: red;
            color: white;
            text-decoration: none;
            border-radius: 4px;
        }

        form {
            background: white;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 8px;
        }

        input {
            width: 100%;
            padding: 8px;
            margin-bottom: 12px;
        }

        button {
            width: 100%;
            padding: 10px;
            background: #007bff;
            border: none;
            color: white;
            cursor: pointer;
        }

        .pagination {
            text-align: center;
            margin-top: 20px;
        }

        .pagination a {
            padding: 6px 10px;
            margin: 5px;
            text-decoration: none;
            background: #ddd;
            color: black;
            border-radius: 4px;
        }

        .pagination a.active {
            background: black;
            color: white;
        }
    </style>

</head>
<body>

    <% if(user) { %>
    <nav>
        <div class="nav-container">
            <div><strong>Mini Instagram</strong></div>
            <div>
                <a href="/">Home</a>
                <a href="/create">Create Post</a>
                <a href="/logout">Logout</a>
            </div>
        </div>
    </nav>
    <% } %>

    <div class="container">
        <%- body %>
    </div>

</body>
</html>